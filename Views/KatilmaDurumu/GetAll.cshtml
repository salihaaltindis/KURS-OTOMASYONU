@model IEnumerable<KURSOTOMASYON.Models.KATILMA_DURUMU>

<h1>KURSA ÖNKAYIT KATILMA DURUMLARI</h1>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KATILMA_DURUMU_REFNO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KATILMA_DURUMU1)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KATILMA_DURUMU_REFNO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KATILMA_DURUMU1)
            </td>
        </tr>
    }

</table>

<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
<script type="text/javascript">
    $(function () {
        $("#footer").hide();
        $("#sidebarCollapse").hide();
        $("#hesapyonet").hide();
    });

</script>